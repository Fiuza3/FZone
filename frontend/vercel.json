{
  "buildCommand": "ROLLUP_NATIVE=false npm install && ROLLUP_NATIVE=false npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ],
  "env": {
    "ROLLUP_NATIVE": "false",
    "NODE_OPTIONS": "--no-node-snapshot"
  }
}