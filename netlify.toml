[build]
  base = "frontend"
  publish = "dist"
  command = "npm install --no-optional && npm run build"
  environment = { NODE_VERSION = "16" }

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200